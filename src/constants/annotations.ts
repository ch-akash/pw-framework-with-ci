export enum AnnotationsType {
  LOGGED_IN = "logged-in-user",
  DESKTOP = "desktop",
  PERFORMANCE = "performance-tests",
}

export enum AnnotationDescription {
  LOGGED_IN = "The user is logged in or required to be logged in",
  DESKTOP = "This is a desktop specific test",
  PERFORMANCE = "This test performs Lighthouse Audit",
}
