import { chromium, test } from "@playwright/test";

test("test", async ({ page }) => {
  // await page.goto("https://bookcart.azurewebsites.net/");
  // await page.goto("https://bookcart.azurewebsites.net/");
  // await page.getByPlaceholder("Search books or authors").fill("harry");
  // await page
  //   .getByRole("option", { name: "Harry Potter and the Chamber" })
  //   .click();
});
